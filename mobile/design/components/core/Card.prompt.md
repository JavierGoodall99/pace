The bento-grid feature card. Default: ash surface, 24px radius, hairline border, lifts + warms border on hover, animated bottom edge-accent line. `media` mode strips the fill for full-bleed photography (pair with a gradient-overlay div you provide).

```jsx
<Card><h3>BORN FROM THE RUN CLUB</h3><p>...</p></Card>
<Card media><img src="..."/><div className="gradient-overlay"/></Card>
```
