/**
 * @startingPoint section="Core" subtitle="Overlay panel — waitlist / auth forms" viewport="700x360"
 */
export interface ModalProps {
  children: React.ReactNode;
  onClose?: () => void;
  width?: number;
}
