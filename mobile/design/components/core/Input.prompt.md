Pill text input — coal fill, hairline border, ember focus ring, mono uppercase placeholder convention (e.g. "YOUR@EMAIL.COM").

```jsx
<Input placeholder="YOUR@EMAIL.COM" value={email} onChange={setEmail} error={invalid}/>
```
