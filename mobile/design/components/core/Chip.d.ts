/**
 * @startingPoint section="Core" subtitle="Toggle pill for sport/discipline & training-window multi-select" viewport="700x120"
 */
export interface ChipProps {
  children: React.ReactNode;
  selected?: boolean;
  onClick?: () => void;
  icon?: React.ReactNode;
}
