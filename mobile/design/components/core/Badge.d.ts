/**
 * @startingPoint section="Core" subtitle="Eyebrow / status pill — cohort labels, verification tags" viewport="700x100"
 */
export interface BadgeProps {
  children: React.ReactNode;
  tone?: "neutral" | "accent" | "solid";
  dot?: boolean;
}
