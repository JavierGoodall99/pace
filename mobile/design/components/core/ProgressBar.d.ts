/**
 * @startingPoint section="Core" subtitle="Step/completion track — onboarding header, compatibility bars" viewport="700x80"
 */
export interface ProgressBarProps {
  value?: number;
  max?: number;
  width?: number;
}
